<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Read the watch's own step count via Health Services (granted by the user in the watch app) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/FitPalWearTheme">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!-- This watch app relies on the paired phone app for AI analysis + the stats snapshot,
             so it is NOT standalone. It is delivered alongside the phone app (same applicationId). -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/FitPalWearTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives the /stats DataItem the phone pushes, to refresh the tile + complications. -->
        <service
            android:name=".WearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/fitpal" />
            </intent-filter>
        </service>

        <!-- Health Services delivers the watch's daily step count here (passive, batched);
             each update is forwarded to the phone so its step total can never miss watch steps. -->
        <service
            android:name=".StepsPassiveService"
            android:exported="true"
            android:permission="com.google.android.wearable.healthservices.permission.PASSIVE_DATA_BINDING" />

        <!-- Water quick-log tile -->
        <service
            android:name=".tile.WaterTileService"
            android:exported="true"
            android:label="@string/water_tile_label"
            android:description="@string/water_tile_description"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_water_preview" />
        </service>

        <!-- Water quick-log complication -->
        <service
            android:name=".complication.WaterComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_water"
            android:label="@string/water_complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

        <!-- Read-only "calories left today" complication -->
        <service
            android:name=".complication.CaloriesComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/calories_complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

        <!-- Fires a water log to the phone from a complication tap. -->
        <receiver
            android:name=".QuickLogReceiver"
            android:exported="false" />

        <!-- No-UI activity the water tile launches to quick-log (tiles can't target a receiver). -->
        <activity
            android:name=".QuickLogActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/FitPalInvisibleTheme" />
    </application>
</manifest>
