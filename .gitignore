# Gradle / build outputs
.gradle/
**/build/
.kotlin/

# Local machine config (SDK path etc. — not portable between machines)
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Packaged artifacts (built from source, not tracked)
*.apk
*.aab

# Signing keys — never commit these
*.jks
*.keystore

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Internal project notes (kept out of the public repo)
/CLAUDE.md
/REDESIGN_PLAN.md
/MODEL_SETUP.md
/DESIGN_SYSTEM.md
/WEAR_SETUP.md
